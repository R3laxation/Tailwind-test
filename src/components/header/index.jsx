import React from 'react';
import {ReactComponent as LogoIcon} from '../../images/logo.svg';

export const Header = () => {
    return (
        <header>
            <LogoIcon/>
        </header>
    );
};
